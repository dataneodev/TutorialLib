<Window
    x:Class="dataneo.TutorialsLibs.WPF.UI.Dialogs.ErrorWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:dataneo.TutorialsLibs.WPF.UI.Dialogs"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:tr="clr-namespace:dataneo.TutorialsLibs.WPF.Translation"
    Title="{x:Static tr:UI.ERROR}"
    SizeToContent="WidthAndHeight"
    Topmost="True"
    WindowStartupLocation="CenterOwner"
    WindowStyle="SingleBorderWindow"
    mc:Ignorable="d">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/dataneo.TutorialsLibs.WPF;component/Resources/Global.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Window.Background>
        <StaticResource ResourceKey="backGround" />
    </Window.Background>
    <Window.Foreground>
        <StaticResource ResourceKey="foreGround" />
    </Window.Foreground>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Viewbox
            Grid.Row="0"
            Grid.Column="0"
            Width="40"
            Margin="0,10,10,10"
            HorizontalAlignment="Center"
            VerticalAlignment="Bottom"
            Panel.ZIndex="1">
            <Path
                Name="phCircle"
                Data="M14.5,25h3v-3h-3V25z M14.5,6v13h3V6H14.5z"
                Stroke="#E6E6E6"
                StrokeThickness="1.4" />
        </Viewbox>
        <Ellipse
            Grid.Row="1"
            Grid.Column="0"
            Width="16"
            Height="16"
            Margin="20,0,0,0"
            HorizontalAlignment="Center"
            VerticalAlignment="Top"
            Stroke="#D72828"
            StrokeThickness="16" />

        <TextBlock
            x:Name="tbErrorMessage"
            Grid.Row="0"
            Grid.RowSpan="2"
            Grid.Column="1"
            MinWidth="250"
            MaxWidth="300"
            Margin="10"
            HorizontalAlignment="Left"
            VerticalAlignment="Center"
            FontSize="13"
            TextWrapping="Wrap" />

        <Button
            x:Name="btOK"
            Grid.Row="2"
            Grid.Column="1"
            Width="100"
            Margin="10"
            HorizontalAlignment="Right"
            Click="btOK_Click"
            Cursor="Hand"
            Style="{StaticResource button}">
            <Button.Content>
                <TextBlock
                    Margin="5"
                    FontSize="14"
                    FontWeight="Bold"
                    Foreground="{StaticResource foreGround}"
                    Text="OK" />
            </Button.Content>
        </Button>
    </Grid>
</Window>
